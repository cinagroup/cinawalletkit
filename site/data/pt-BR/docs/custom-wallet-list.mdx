---
title: Lista Personalizada de Carteiras
description: Personalizando a lista de carteiras
---

# Lista Personalizada de Carteiras

## Personalizando a lista de carteiras

Você pode importar carteiras individuais de `'@cinagroup/CinaWalletKit/wallets'` juntamente com a função `connectorsForWallets` para criar sua própria lista de carteiras com os conectores necessários. Dessa forma, você tem total controle sobre quais carteiras exibir e em qual ordem.

Por exemplo, você pode optar por mostrar apenas a Rainbow junto com carteiras genéricas de fallback.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, walletConnectWallet],
    },
  ],
  {
    appName: 'My CinaWalletKit App',
    projectId: 'YOUR_PROJECT_ID',
  }
);
```

Você pode então passar seus conectores para a função `createConfig` do Wagmi.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import { createConfig } from 'wagmi';

const connectors = connectorsForWallets(/* ... */);

const config = createConfig({
  connectors,
  {/* Wagmi config */}
});

const queryClient = new QueryClient();

const App = () => (
  <WagmiProvider config={config}>
    <QueryClientProvider client={queryClient}>
      <CinaWalletKitProvider {...etc}>
        {/* Your App */}
      </CinaWalletKitProvider>
    </QueryClientProvider>
  </WagmiProvider>
);
```

### Carteiras Integradas

As seguintes carteiras são fornecidas através do objeto `wallet`.

#### Carteiras Genéricas

As seguintes carteiras são destinadas a métodos de conexão genéricos em vez de aplicativos específicos. Como resultado, é recomendado que essas carteiras sejam sempre incluídas.

##### WalletConnect

Esta é uma opção de carteira fallback projetada para carteiras baseadas no WalletConnect que não foram fornecidas por outra carteira na lista.

**Recomenda-se que você sempre inclua esta carteira na lista para garantir que todas as carteiras baseadas no WalletConnect sejam suportadas.**

```tsx
import { walletConnectWallet } from '@cinagroup/CinaWalletKit/wallets';
```

##### Carteira Injetada

Esta é uma opção de carteira fallback projetada para cenários onde `window.ethereum` existe, mas não foi fornecida por outra carteira na lista. Esta carteira se ocultará automaticamente da lista quando o fallback não for necessário ou se não houver uma carteira injetada disponível.

**Recomenda-se que você sempre inclua esta carteira na lista para garantir que todas as carteiras injetadas sejam suportadas.**

```tsx
import { injectedWallet } from '@cinagroup/CinaWalletKit/wallets';
```

##### Carteira Safe

Para dApps que suportam Safe Apps, permitindo que os usuários conectem facilmente com seu cofre Safe ou multi-sig, inclua a opção `safeWallet` para conexões automáticas. A opção Safe só aparecerá no ambiente do navegador Safe Wallet.

```tsx
import { safeWallet } from '@cinagroup/CinaWalletKit/wallets';
```

### Carteiras Específicas

As seguintes carteiras são fornecidas através do objeto `wallet` (em ordem alfabética).

#### Carteira 1inch

```tsx
import { oneInchWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Anchorage Digital

```tsx
import { anchorageDigitalWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Backpack

```tsx
import { backpackWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Base (anteriormente Coinbase Wallet)

Este conector de carteira suporta o Base, a evolução do Coinbase Smart Wallet. Ele oferece autenticação perfeita e recursos avançados como subcontas e permissões de gastos.

As opções de configuração estão alinhadas com a [referência do Base SDK](https://docs.base.org/base-account/reference/core/createBaseAccount).

> `baseAccount` e `coinbaseWallet` permanecem disponíveis como aliases retrocompatíveis.

```tsx
import { base } from '@cinagroup/CinaWalletKit/wallets';
import { base as baseChain } from 'wagmi/chains';

// Configure attribution (choose one approach)
base.preference = {
  attribution: { auto: true }, // Smart Wallet generates origin-based identifier
};

// Or use custom attribution
base.preference = {
  attribution: { dataSuffix: '0x1234567890abcdef' }, // 16-byte hex identifier
};

// Configure sub-accounts
base.subAccounts = {
  creation: 'on-connect', // or 'manual'
  defaultAccount: 'sub', // or 'universal'
  funding: 'spend-permissions', // or 'manual'
};

// Configure paymaster URLs for gasless transactions
base.paymasterUrls = {
  [baseChain.id]: 'https://api.developer.coinbase.com/rpc/v1/base/...',
};
```

#### BeraSig

```tsx
import { berasigWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Melhor Carteira

```tsx
import { bestWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Bifrost

```tsx
import { bifrostWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Binance Web3

```tsx
import { binanceWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitget

```tsx
import { bitgetWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitski

```tsx
import { bitskiWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitverse

```tsx
import { bitverseWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Bloom

```tsx
import { bloomWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Brave

```tsx
import { braveWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Bybit

```tsx
import { bybitWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Coin98

```tsx
import { coin98Wallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Compass

```tsx
import { compassWallet } from '@cinagroup/CinaWalletKit/compassWallet';
```

#### Core

```tsx
import { coreWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira CTRL

```tsx
import { ctrlWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Dawn

```tsx
import { dawnWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Enkrypt

```tsx
import { enkryptWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### FoxWallet

```tsx
import { foxWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Frame

```tsx
import { frameWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Frontier

```tsx
import { frontierWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Gate

```tsx
import { gateWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Gemini

Este conector de carteira inteligente permite autenticação perfeita através de passkeys em keys.gemini.com - sem downloads, extensões ou chaves API necessárias. Funciona direto de fábrica.

O parâmetro `appName` é obrigatório para identificar seu dApp, e `appIcon` é opcional para a personalização da experiência de conexão.

```tsx
import { geminiWallet } from '@cinagroup/CinaWalletKit/wallets';

const gemini = geminiWallet({
  appName: 'My App',
  appIcon: 'https://my-app.com/logo.png', // Optional
});
```

#### imToken

```tsx
import { imTokenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira ioPay

```tsx
import { iopayWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Kaia

```tsx
import { kaiaWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Kaikas

```tsx
import { kaikasWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Kraken

```tsx
import { krakenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Kresus

```tsx
import { kresusWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Ledger Live

```tsx
import { ledgerWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Magic Eden

```tsx
import { magicEdenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### MetaMask

```tsx
import { metaMaskWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira MEW

```tsx
import { mewWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira MeCo

```tsx
import { mecoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Nova

```tsx
import { novaWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira OKX

```tsx
import { okxWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Omni

```tsx
import { omniWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### OneKey

```tsx
import { oneKeyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### ParaSwap

```tsx
import { paraSwapWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Phantom

```tsx
import { phantomWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Porto

```tsx
import { portoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Rabby

```tsx
import { rabbyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Rainbow

```tsx
import { rainbowWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Ramper

```tsx
import { ramperWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Pronto

```tsx
import { readyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Ronin

```tsx
import { roninWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Safeheron

```tsx
import { safeheronWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Taho

```tsx
import { tahoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Talisman

```tsx
import { talismanWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Tokenary

```tsx
import { tokenaryWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### TokenPocket

```tsx
import { tokenPocketWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Trust Wallet

```tsx
import { trustWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Carteira Uniswap

```tsx
import { uniswapWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Valora

```tsx
import { valoraWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Wigwam

```tsx
import { wigwamWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### xPortal

```tsx
import { xPortalWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Zerion

```tsx
import { zerionWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### ZilPay

```tsx
import { zilPayWallet } from '@cinagroup/CinaWalletKit/wallets';
```

### Exemplos

#### Ordenação

Aqui estão alguns exemplos de como exibir diferentes carteiras, em diferentes ordens.

Mostre MetaMask junto com carteiras genéricas de fallback.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  metaMaskWallet,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [metaMaskWallet, walletConnectWallet],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

Mostrar Rainbow, MetaMask e Base juntamente com carteiras genéricas de fallback.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  metaMaskWallet,
  base,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Suggested',
      wallets: [
        rainbowWallet,
        metaMaskWallet,
        base,
        walletConnectWallet,
      ],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

> Lembrete: A ordem do array `wallets` define a ordem em que as carteiras serão exibidas na UI.

#### Múltiplos grupos

Você pode usar a chave `groupName` para nomear diferentes grupos de carteiras. Isso é útil se você quiser comunicar aos seus usuários quais carteiras você recomenda, bem como outras possíveis carteiras.

Recomende Rainbow e MetaMask, mas também ofereça Base juntamente com carteiras genéricas de fallback.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  metaMaskWallet,
  base,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, metaMaskWallet],
    },
    {
      groupName: 'Others',
      wallets: [base, walletConnectWallet],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```
