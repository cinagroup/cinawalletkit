---
title: Cüzdan Butonu
description: Cüzdan Butonunu Kullanma ve Özelleştirme
---

# Cüzdan Butonu

> Not: `WalletButton` şu anda EIP-1193 cüzdan standardına dayanmaktadır, ancak yakında EIP-6963'ü destekleyecektir.

Yeni `WalletButton` bileşeni, özel cüzdan listesi uygulamalarına sahip dApp'lerin CinaWalletKit'e geçmesini ve bakım gerektirmeyen tüm avantajlarından yararlanmasını sağlar.

```tsx
import { WalletButton } from '@cinagroup/CinaWalletKit';

<WalletButton wallet="rainbow" />
<WalletButton wallet="metamask" />
<WalletButton wallet="base" />
// Backwards-compatible wallet ids: "baseAccount" and "coinbaseWallet"
```

`ConnectButton` gibi, `WalletButton.Custom` bileşeni, özel uygulamalar ve stil vermek için kullanılabilir.

```tsx
<WalletButton.Custom wallet="rainbow">
  {({ ready, connect }) => {
    return (
      <button
        type="button"
        disabled={!ready}
        onClick={connect}
      >
        Connect Rainbow
      </button>
    );
  }}
</WalletButton.Custom>
```

Çoğu dApp, [ConnectButton](https://www.CinaWalletKit.com/docs/connect-button) ile en iyi şekilde çalışır. `WalletButton`'ın benimsenmesi ve kullanım durumları hakkında daha fazla bilgi için [buradaki](https://www.CinaWalletKit.com/docs/wallet-button) belgelere bakın.

#### Demo

Bu CinaWalletKit giriş noktasının nasıl göründüğünü görmek için aşağıdaki düğmelere basın.

<WalletButtonDemo />
