---
title: Maklumat Aplikasi Tersuai
description: Menyesuaikan maklumat aplikasi anda
---

# Maklumat Aplikasi Tersuai

## Menyesuaikan maklumat aplikasi anda

Anda boleh memasukkan maklumat aplikasi anda dalam prop `appInfo` untuk `CinaWalletKitProvider`. Ciri-ciri yang boleh anda ubah ialah nama aplikasi anda (`appName`) dan pautan di mana butang "Pelajari Lebih Lanjut" dalam mod sambungan mengarah (`learnMoreUrl`):

```tsx
import { CinaWalletKitProvider } from '@cinagroup/CinaWalletKit';

const App = () => {
  return (
    <CinaWalletKitProvider
      appInfo={{
        appName: 'CinaWalletKit Demo',
        learnMoreUrl: 'https://learnaboutcryptowallets.example',
      }}
      {...etc}
    >
      {/* ... */}
    </CinaWalletKitProvider>
  );
};
```

### Tambah Penafian

Anda boleh menyediakan penafian anda sendiri untuk dipaparkan di bahagian bawah mod sambungan. Ciri `disclaimer` dalam `appInfo` mengambil `DisclaimerComponent` dengan argumen `Text` dan `Link`. Anda boleh menggunakannya untuk membina penafian anda sendiri yang mewarisi sistem tema. Kami mengekspor jenis `DisclaimerComponent` untuk pengguna TypeScript:

```tsx
import {
  CinaWalletKitProvider,
  DisclaimerComponent,
} from '@cinagroup/CinaWalletKit';

const Disclaimer: DisclaimerComponent = ({ Text, Link }) => (
  <Text>
    By connecting your wallet, you agree to the{' '}
    <Link href="https://termsofservice.xyz">Terms of Service</Link> and
    acknowledge you have read and understand the protocol{' '}
    <Link href="https://disclaimer.xyz">Disclaimer</Link>
  </Text>
);

const App = () => {
  return (
    <CinaWalletKitProvider
      appInfo={{
        appName: 'CinaWalletKit Demo',
        disclaimer: Disclaimer,
      }}
      {...etc}
    >
      {/* ... */}
    </CinaWalletKitProvider>
  );
};
```
