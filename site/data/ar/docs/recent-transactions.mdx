---
title: المعاملات الأخيرة
description: عرض معاملاتك الأخيرة
---

# المعاملات الأخيرة

## عرض معاملاتك الأخيرة

يمكنك اختيار عرض المعاملات الأخيرة داخل نافذة حساب CinaWalletKit. لاحظ أن جميع المعاملات تُخزن محليًا ويجب تسجيلها يدويًا مع CinaWalletKit ليتم عرضها.

ستعرض تنفيذ الافتراضي لـ `ConnectButton` مؤشر تحميل حول صورة المستخدم الرمزية إذا كانت هناك معاملات قيد الانتظار. يمكن للتنفيذات المخصصة لـ `ConnectButton` إعادة إنشاء هذا السلوك عبر خاصية `account.hasPendingTransactions` التي تُمرر لوظيفة العرض لديك.

لاستخدام هذه الميزة، قم أولاً بتمكين خيار `showRecentTransactions` في `CinaWalletKitProvider`.

```tsx
import { CinaWalletKitProvider } from '@cinagroup/CinaWalletKit';

const App = () => {
  return (
    <CinaWalletKitProvider showRecentTransactions={true} {...etc}>
      {/* ... */}
    </CinaWalletKitProvider>
  );
};
```

يمكن بعد ذلك تسجيل المعاملات مع CinaWalletKit باستخدام الخطاف `useAddRecentTransaction`.

```tsx
import { useAddRecentTransaction } from '@cinagroup/CinaWalletKit';

export default () => {
  const addRecentTransaction = useAddRecentTransaction();

  return (
    <button
      onClick={() => {
        addRecentTransaction({
          hash: '0x...',
          description: '...',
        });
      }}
    >
      Add recent transaction
    </button>
  );
};
```

بمجرد تسجيل معاملة مع CinaWalletKit، سيتم تحديث حالتها عند الاكتمال.

بشكل افتراضي، يُعتبر أن المعاملة انتهت بمجرد تعدين كتلة واحدة فوق الكتلة التي تم تعدين المعاملة فيها، ولكن يمكن تكوين ذلك بتحديد قيمة `confirmations` مخصصة.

```tsx
import { useAddRecentTransaction } from '@cinagroup/CinaWalletKit';

export default () => {
  const addRecentTransaction = useAddRecentTransaction();

  return (
    <button
      onClick={() => {
        addRecentTransaction({
          hash: '0x...',
          description: '...',
          confirmations: 100,
        });
      }}
    >
      Add recent transaction
    </button>
  );
};
```
