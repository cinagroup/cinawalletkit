---
title: Lista de Billeteras Personalizadas
description: Personalizando la lista de billeteras
---

# Lista de Billeteras Personalizadas

## Personalizando la lista de billeteras

Puedes importar billeteras individuales desde `'@cinagroup/CinaWalletKit/wallets'` junto con la función `connectorsForWallets` para construir tu propia lista de billeteras con sus conectores necesarios. De esta manera, tienes control total sobre qué billeteras mostrar y en qué orden.

Por ejemplo, puedes optar por mostrar solo Rainbow junto con billeteras genéricas de respaldo.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, walletConnectWallet],
    },
  ],
  {
    appName: 'My CinaWalletKit App',
    projectId: 'YOUR_PROJECT_ID',
  }
);
```

Luego puedes pasar tus conectores a `createConfig` de Wagmi.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import { createConfig } from 'wagmi';

const connectors = connectorsForWallets(/* ... */);

const config = createConfig({
  connectors,
  {/* Wagmi config */}
});

const queryClient = new QueryClient();

const App = () => (
  <WagmiProvider config={config}>
    <QueryClientProvider client={queryClient}>
      <CinaWalletKitProvider {...etc}>
        {/* Your App */}
      </CinaWalletKitProvider>
    </QueryClientProvider>
  </WagmiProvider>
);
```

### Billeteras Integradas

Las siguientes billeteras se proporcionan a través del objeto `wallet`.

#### Billeteras Genéricas

Las siguientes billeteras están adaptadas a métodos de conexión genéricos en lugar de aplicaciones específicas. Como resultado, se recomienda que estas billeteras siempre estén incluidas.

##### WalletConnect

Esta es una opción de billetera de respaldo diseñada para billeteras basadas en WalletConnect que no han sido proporcionadas por otra billetera en la lista.

**Se recomienda que siempre incluyas esta billetera en la lista para asegurar que todas las billeteras basadas en WalletConnect sean compatibles.**

```tsx
import { walletConnectWallet } from '@cinagroup/CinaWalletKit/wallets';
```

##### Billetera Inyectada

Esta es una opción de billetera de respaldo diseñada para escenarios donde `window.ethereum` existe pero no ha sido proporcionado por otra billetera en la lista. Esta billetera se ocultará automáticamente de la lista cuando el respaldo no sea necesario o si no hay una billetera inyectada disponible.

**Se recomienda que siempre incluyas esta billetera en la lista para asegurar que todas las billeteras inyectadas sean compatibles.**

```tsx
import { injectedWallet } from '@cinagroup/CinaWalletKit/wallets';
```

##### Billetera Segura

Para dApps que soportan Safe Apps para permitir a los usuarios conectarse fácilmente con su bóveda segura o multi-firma, incluye la opción `safeWallet` para conexiones automáticas. La opción Segura solo aparecerá en el entorno del navegador de Safe Wallet.

```tsx
import { safeWallet } from '@cinagroup/CinaWalletKit/wallets';
```

### Billeteras Específicas

Las siguientes billeteras se proporcionan a través del objeto `wallet` (en orden alfabético).

#### 1inch Billetera

```tsx
import { oneInchWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Anchorage Digital

```tsx
import { anchorageDigitalWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Backpack

```tsx
import { backpackWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Base (anteriormente Coinbase Wallet)

Este conector de billetera es compatible con Base, la evolución de Coinbase Smart Wallet. Proporciona autenticación sin problemas y características mejoradas, como sub-cuentas y permisos de gasto.

Las opciones de configuración se alinean con la [referencia del SDK de Base](https://docs.base.org/base-account/reference/core/createBaseAccount).

> `baseAccount` y `coinbaseWallet` siguen disponibles como alias compatibles con versiones anteriores.

```tsx
import { base } from '@cinagroup/CinaWalletKit/wallets';
import { base as baseChain } from 'wagmi/chains';

// Configure attribution (choose one approach)
base.preference = {
  attribution: { auto: true }, // Smart Wallet generates origin-based identifier
};

// Or use custom attribution
base.preference = {
  attribution: { dataSuffix: '0x1234567890abcdef' }, // 16-byte hex identifier
};

// Configure sub-accounts
base.subAccounts = {
  creation: 'on-connect', // or 'manual'
  defaultAccount: 'sub', // or 'universal'
  funding: 'spend-permissions', // or 'manual'
};

// Configure paymaster URLs for gasless transactions
base.paymasterUrls = {
  [baseChain.id]: 'https://api.developer.coinbase.com/rpc/v1/base/...',
};
```

#### BeraSig

```tsx
import { berasigWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Mejor Billetera

```tsx
import { bestWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Billetera Bifrost

```tsx
import { bifrostWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Binance Billetera Web3

```tsx
import { binanceWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitget

```tsx
import { bitgetWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitski

```tsx
import { bitskiWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitverse

```tsx
import { bitverseWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bloom Billetera

```tsx
import { bloomWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Brave Billetera

```tsx
import { braveWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bybit Billetera

```tsx
import { bybitWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Coin98 Billetera

```tsx
import { coin98Wallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Compass Billetera

```tsx
import { compassWallet } from '@cinagroup/CinaWalletKit/compassWallet';
```

#### Core

```tsx
import { coreWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Billetera CTRL

```tsx
import { ctrlWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Dawn Billetera

```tsx
import { dawnWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Enkrypt Billetera

```tsx
import { enkryptWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### FoxWallet

```tsx
import { foxWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Frame Billetera

```tsx
import { frameWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Frontier Billetera

```tsx
import { frontierWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Gate Billetera

```tsx
import { gateWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Wallet de Gemini

Este conector de billetera inteligente permite una autenticación sin problemas a través de claves de acceso en keys.gemini.com - no se requieren descargas, extensiones ni claves API. Funciona perfectamente desde el principio.

El parámetro `appName` es obligatorio para identificar tu dApp, y `appIcon` es opcional para la personalización de la experiencia de conexión.

```tsx
import { geminiWallet } from '@cinagroup/CinaWalletKit/wallets';

const gemini = geminiWallet({
  appName: 'My App',
  appIcon: 'https://my-app.com/logo.png', // Optional
});
```

#### imToken

```tsx
import { imTokenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### ioPay Billetera

```tsx
import { iopayWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Kaia Billetera

```tsx
import { kaiaWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Kaikas

```tsx
import { kaikasWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Kraken Billetera

```tsx
import { krakenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Kresus Billetera

```tsx
import { kresusWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Ledger Live

```tsx
import { ledgerWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Magic Eden Billetera

```tsx
import { magicEdenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### MetaMask

```tsx
import { metaMaskWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### MEW Wallet

```tsx
import { mewWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Billetera MeCo

```tsx
import { mecoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Nova Billetera

```tsx
import { novaWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### OKX Billetera

```tsx
import { okxWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Omni

```tsx
import { omniWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### OneKey

```tsx
import { oneKeyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### ParaSwap

```tsx
import { paraSwapWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Phantom

```tsx
import { phantomWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Porto

```tsx
import { portoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Rabby Billetera

```tsx
import { rabbyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Rainbow

```tsx
import { rainbowWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Ramper Billetera

```tsx
import { ramperWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Listo

```tsx
import { readyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Ronin Wallet

```tsx
import { roninWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Safeheron

```tsx
import { safeheronWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Taho

```tsx
import { tahoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Talisman

```tsx
import { talismanWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Tokenary

```tsx
import { tokenaryWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### TokenPocket

```tsx
import { tokenPocketWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Trust Wallet

```tsx
import { trustWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Uniswap Wallet

```tsx
import { uniswapWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Valora

```tsx
import { valoraWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Wigwam

```tsx
import { wigwamWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### xPortal

```tsx
import { xPortalWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Zerion

```tsx
import { zerionWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### ZilPay

```tsx
import { zilPayWallet } from '@cinagroup/CinaWalletKit/wallets';
```

### Ejemplos

#### Orden

Aquí hay algunos ejemplos de presentación de diferentes billeteras, en diferente orden.

Mostrar MetaMask junto con billeteras genéricas de respaldo.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  metaMaskWallet,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [metaMaskWallet, walletConnectWallet],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

Mostrar Rainbow, MetaMask y Base junto con billeteras de reserva genéricas.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  metaMaskWallet,
  base,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Suggested',
      wallets: [
        rainbowWallet,
        metaMaskWallet,
        base,
        walletConnectWallet,
      ],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

> Recordatorio: el orden del array `wallets` define el orden en el que las billeteras se mostrarán en la interfaz de usuario.

#### Múltiples grupos

Puedes usar la clave `groupName` para nombrar diferentes grupos de billeteras. Esto es útil si deseas comunicar a tus usuarios qué billeteras recomiendas, así como otras billeteras posibles.

Recomendar Rainbow y MetaMask, pero también ofrecer Base junto con billeteras genéricas de reserva.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  metaMaskWallet,
  base,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, metaMaskWallet],
    },
    {
      groupName: 'Others',
      wallets: [base, walletConnectWallet],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```
