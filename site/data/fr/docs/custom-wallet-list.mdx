---
title: Liste de Portefeuilles Personnalisée
description: Personnalisation de la liste de portefeuilles
---

# Liste de Portefeuilles Personnalisée

## Personnalisation de la liste de portefeuilles

Vous pouvez importer des portefeuilles individuels depuis `'@cinagroup/CinaWalletKit/wallets'` avec la fonction `connectorsForWallets` pour construire votre propre liste de portefeuilles avec leurs connecteurs nécessaires. De cette manière, vous avez un contrôle total sur les portefeuilles à afficher et dans quel ordre.

Par exemple, vous pouvez choisir de n'afficher que Rainbow ainsi que des portefeuilles génériques de secours.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, walletConnectWallet],
    },
  ],
  {
    appName: 'My CinaWalletKit App',
    projectId: 'YOUR_PROJECT_ID',
  }
);
```

Vous pouvez ensuite passer vos connecteurs à la fonction `createConfig` de Wagmi.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import { createConfig } from 'wagmi';

const connectors = connectorsForWallets(/* ... */);

const config = createConfig({
  connectors,
  {/* Wagmi config */}
});

const queryClient = new QueryClient();

const App = () => (
  <WagmiProvider config={config}>
    <QueryClientProvider client={queryClient}>
      <CinaWalletKitProvider {...etc}>
        {/* Your App */}
      </CinaWalletKitProvider>
    </QueryClientProvider>
  </WagmiProvider>
);
```

### Portefeuilles intégrés

Les portefeuilles suivants sont fournis via l'objet `wallet`.

#### Portefeuilles Génériques

Les portefeuilles suivants sont limités à des méthodes de connexion génériques plutôt qu'à des applications spécifiques. Il est donc recommandé d'inclure toujours ces portefeuilles.

##### WalletConnect

Ceci est une option de portefeuille de secours conçue pour les portefeuilles basés sur WalletConnect qui n'ont pas été fournis par un autre portefeuille de la liste.

**Il est recommandé d'inclure toujours ce portefeuille dans la liste pour garantir le support de tous les portefeuilles basés sur WalletConnect.**

```tsx
import { walletConnectWallet } from '@cinagroup/CinaWalletKit/wallets';
```

##### Portefeuille Injecté

Ceci est une option de portefeuille de secours conçue pour les scénarios où `window.ethereum` existe mais n'a pas été fourni par un autre portefeuille de la liste. Ce portefeuille se cachera automatiquement de la liste lorsque le secours n'est pas nécessaire ou s'il n'y a aucun portefeuille injecté disponible.

**Il est recommandé d'inclure toujours ce portefeuille dans la liste pour garantir le support de tous les portefeuilles injectés.**

```tsx
import { injectedWallet } from '@cinagroup/CinaWalletKit/wallets';
```

##### Portefeuille Sûr

Pour les dApps qui prennent en charge les Safe Apps permettant aux utilisateurs de se connecter facilement à leur coffre-fort Safe ou multi-sig, incluez l'option `safeWallet` pour des connexions automatiques. L'option Safe n'apparaîtra que dans l'environnement du navigateur Safe Wallet.

```tsx
import { safeWallet } from '@cinagroup/CinaWalletKit/wallets';
```

### Portefeuilles Spécifiques

Les portefeuilles suivants sont fournis via l'objet `wallet` (par ordre alphabétique).

#### Portefeuille 1inch

```tsx
import { oneInchWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Anchorage Digital

```tsx
import { anchorageDigitalWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Backpack

```tsx
import { backpackWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Base (anciennement Coinbase Wallet)

Ce connecteur de portefeuille prend en charge Base, l'évolution de Coinbase Smart Wallet. Il offre une authentification transparente et des fonctionnalités améliorées telles que des sous-comptes et des autorisations de dépenses.

Les options de configuration s'alignent sur la [référence SDK Base](https://docs.base.org/base-account/reference/core/createBaseAccount).

> `baseAccount` et `coinbaseWallet` restent disponibles en tant qu'alias rétrocompatibles.

```tsx
import { base } from '@cinagroup/CinaWalletKit/wallets';
import { base as baseChain } from 'wagmi/chains';

// Configure attribution (choose one approach)
base.preference = {
  attribution: { auto: true }, // Smart Wallet generates origin-based identifier
};

// Or use custom attribution
base.preference = {
  attribution: { dataSuffix: '0x1234567890abcdef' }, // 16-byte hex identifier
};

// Configure sub-accounts
base.subAccounts = {
  creation: 'on-connect', // or 'manual'
  defaultAccount: 'sub', // or 'universal'
  funding: 'spend-permissions', // or 'manual'
};

// Configure paymaster URLs for gasless transactions
base.paymasterUrls = {
  [baseChain.id]: 'https://api.developer.coinbase.com/rpc/v1/base/...',
};
```

#### BeraSig

```tsx
import { berasigWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Meilleur Portefeuille

```tsx
import { bestWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Bifrost

```tsx
import { bifrostWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Binance Web3

```tsx
import { binanceWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitget

```tsx
import { bitgetWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitski

```tsx
import { bitskiWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitverse

```tsx
import { bitverseWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Bloom

```tsx
import { bloomWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Brave

```tsx
import { braveWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Bybit

```tsx
import { bybitWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Coin98

```tsx
import { coin98Wallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Compass

```tsx
import { compassWallet } from '@cinagroup/CinaWalletKit/compassWallet';
```

#### Core

```tsx
import { coreWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille CTRL

```tsx
import { ctrlWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Dawn

```tsx
import { dawnWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Enkrypt

```tsx
import { enkryptWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### FoxWallet

```tsx
import { foxWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Frame

```tsx
import { frameWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Frontier

```tsx
import { frontierWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Gate

```tsx
import { gateWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Gemini

Ce connecteur de portefeuille intelligent permet une authentification transparente via des passes à keys.gemini.com - pas de téléchargements, d’extensions ou de clés API requis. Cela fonctionne immédiatement.

Le paramètre `appName` est requis pour identifier votre dApp, et `appIcon` est facultatif pour personnaliser votre expérience de connexion.

```tsx
import { geminiWallet } from '@cinagroup/CinaWalletKit/wallets';

const gemini = geminiWallet({
  appName: 'My App',
  appIcon: 'https://my-app.com/logo.png', // Optional
});
```

#### imToken

```tsx
import { imTokenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille ioPay

```tsx
import { iopayWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Kaia

```tsx
import { kaiaWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Kaikas

```tsx
import { kaikasWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Kraken

```tsx
import { krakenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Kresus

```tsx
import { kresusWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Ledger Live

```tsx
import { ledgerWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Magic Eden

```tsx
import { magicEdenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### MetaMask

```tsx
import { metaMaskWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### MEW Wallet

```tsx
import { mewWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### MeCo Portefeuille

```tsx
import { mecoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Nova

```tsx
import { novaWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille OKX

```tsx
import { okxWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Omni

```tsx
import { omniWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### OneKey

```tsx
import { oneKeyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### ParaSwap

```tsx
import { paraSwapWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Phantom

```tsx
import { phantomWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Porto

```tsx
import { portoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Rabby

```tsx
import { rabbyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Rainbow

```tsx
import { rainbowWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Portefeuille Ramper

```tsx
import { ramperWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Prêt

```tsx
import { readyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Ronin Wallet

```tsx
import { roninWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Safeheron

```tsx
import { safeheronWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Taho

```tsx
import { tahoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Talisman

```tsx
import { talismanWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Tokenary

```tsx
import { tokenaryWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### TokenPocket

```tsx
import { tokenPocketWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Trust Wallet

```tsx
import { trustWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Uniswap Wallet

```tsx
import { uniswapWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Valora

```tsx
import { valoraWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Wigwam

```tsx
import { wigwamWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### xPortal

```tsx
import { xPortalWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Zerion

```tsx
import { zerionWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### ZilPay

```tsx
import { zilPayWallet } from '@cinagroup/CinaWalletKit/wallets';
```

### Exemples

#### Ordonnance

Voici quelques exemples d'affichage de différents portefeuilles, dans différents ordres.

Afficher MetaMask ainsi que les portefeuilles génériques de secours.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  metaMaskWallet,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [metaMaskWallet, walletConnectWallet],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

Affichez Rainbow, MetaMask et Base ainsi que les portefeuilles génériques de secours.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  metaMaskWallet,
  base,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Suggested',
      wallets: [
        rainbowWallet,
        metaMaskWallet,
        base,
        walletConnectWallet,
      ],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

> Rappel : L'ordre du tableau `wallets` définit l'ordre dans lequel les portefeuilles seront affichés dans l'interface utilisateur.

#### Multiples groupes

Vous pouvez utiliser la clé `groupName` pour nommer différents groupes de portefeuilles. C'est utile si vous souhaitez indiquer à vos utilisateurs quels portefeuilles vous recommandez, ainsi que d'autres portefeuilles possibles.

Recommandez Rainbow et MetaMask, mais proposez aussi Base ainsi que des portefeuilles génériques de secours.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  metaMaskWallet,
  base,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, metaMaskWallet],
    },
    {
      groupName: 'Others',
      wallets: [base, walletConnectWallet],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```
