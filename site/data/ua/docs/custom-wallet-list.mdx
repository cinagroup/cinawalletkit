---
title: Користувацький список гаманців
description: Настроювання списку гаманців
---

# Користувацький список гаманців

## Настроювання списку гаманців

Ви можете імпортувати індивідуальні гаманці з `'@cinagroup/CinaWalletKit/wallets'` разом з функцією `connectorsForWallets`, щоб створити власний список гаманців з необхідними підключеннями. Таким чином, ви матимете повний контроль над тим, які гаманці показувати, і в якому порядку.

Наприклад, ви можете обрати показ лише Rainbow разом з універсальними запасними гаманцями.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, walletConnectWallet],
    },
  ],
  {
    appName: 'My CinaWalletKit App',
    projectId: 'YOUR_PROJECT_ID',
  }
);
```

Після цього ви можете передати свої підключення до `createConfig` Wagmi.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import { createConfig } from 'wagmi';

const connectors = connectorsForWallets(/* ... */);

const config = createConfig({
  connectors,
  {/* Wagmi config */}
});

const queryClient = new QueryClient();

const App = () => (
  <WagmiProvider config={config}>
    <QueryClientProvider client={queryClient}>
      <CinaWalletKitProvider {...etc}>
        {/* Your App */}
      </CinaWalletKitProvider>
    </QueryClientProvider>
  </WagmiProvider>
);
```

### Вбудовані гаманці

Наступні гаманці надаються через об'єкт `wallet`.

#### Універсальні гаманці

Наступні гаманці призначені для універсальних методів підключення, а не для конкретних додатків. В результаті, рекомендується завжди включати ці гаманці.

##### WalletConnect

Це запасний варіант гаманця, призначений для гаманців, які базуються на WalletConnect і не надані іншим гаманцем у списку.

**Рекомендується завжди включати цей гаманець в список, щоб гарантувати підтримку всіх гаманців, що базуються на WalletConnect.**

```tsx
import { walletConnectWallet } from '@cinagroup/CinaWalletKit/wallets';
```

##### Інжектований гаманець

Це запасний варіант гаманця, призначений для сценаріїв, коли `window.ethereum` існує, але не наданий іншим гаманцем у списку. Цей гаманець автоматично приховає себе зі списку, коли запасний варіант не потрібний або якщо відсутній інжектований гаманець.

**Рекомендується завжди включати цей гаманець у список, щоб гарантувати підтримку всіх інжектованих гаманців.**

```tsx
import { injectedWallet } from '@cinagroup/CinaWalletKit/wallets';
```

##### Безпечний гаманець

Для дАпп, які підтримують Safe Apps, щоб дозволити користувачам легко підключатися до свого Safe vault або мульти-підпису, включайте опцію `safeWallet` для автоматичних підключень. Опція Safe з'явиться тільки в середовищі браузера Safe Wallet.

```tsx
import { safeWallet } from '@cinagroup/CinaWalletKit/wallets';
```

### Специфічні гаманці

Наступні гаманці надаються через об'єкт `wallet` (в алфавітному порядку).

#### 1inch Гаманець

```tsx
import { oneInchWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Анкоридж Діджитал

```tsx
import { anchorageDigitalWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Рюкзак

```tsx
import { backpackWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Base (колишній Coinbase Wallet)

Цей конектор гаманця підтримує Base, наступний етап розвитку Coinbase Smart Wallet. Забезпечує безперебійну автентифікацію та розширені функції, такі як підрахункові акаунти та дозвіл на витрати.

Параметри конфігурації відповідають [документації Base SDK](https://docs.base.org/base-account/reference/core/createBaseAccount).

> `baseAccount` та `coinbaseWallet` залишаються доступними як зворотно-сумісні псевдоніми.

```tsx
import { base } from '@cinagroup/CinaWalletKit/wallets';
import { base as baseChain } from 'wagmi/chains';

// Configure attribution (choose one approach)
base.preference = {
  attribution: { auto: true }, // Smart Wallet generates origin-based identifier
};

// Or use custom attribution
base.preference = {
  attribution: { dataSuffix: '0x1234567890abcdef' }, // 16-byte hex identifier
};

// Configure sub-accounts
base.subAccounts = {
  creation: 'on-connect', // or 'manual'
  defaultAccount: 'sub', // or 'universal'
  funding: 'spend-permissions', // or 'manual'
};

// Configure paymaster URLs for gasless transactions
base.paymasterUrls = {
  [baseChain.id]: 'https://api.developer.coinbase.com/rpc/v1/base/...',
};
```

#### BeraSig

```tsx
import { berasigWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Найкращий гаманець

```tsx
import { bestWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Гаманець Bifrost

```tsx
import { bifrostWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Binance Web3 Гаманець

```tsx
import { binanceWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitget

```tsx
import { bitgetWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitski

```tsx
import { bitskiWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bitverse

```tsx
import { bitverseWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bloom Гаманець

```tsx
import { bloomWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Brave Гаманець

```tsx
import { braveWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Bybit Гаманець

```tsx
import { bybitWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Coin98 Гаманець

```tsx
import { coin98Wallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Compass Гаманець

```tsx
import { compassWallet } from '@cinagroup/CinaWalletKit/compassWallet';
```

#### Core

```tsx
import { coreWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### CTRL Wallet

```tsx
import { ctrlWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Dawn Гаманець

```tsx
import { dawnWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Enkrypt Гаманець

```tsx
import { enkryptWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### FoxWallet

```tsx
import { foxWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Frame Гаманець

```tsx
import { frameWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Frontier Гаманець

```tsx
import { frontierWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Gate Гаманець

```tsx
import { gateWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Кам'янецький гаманець

Цей розумний конектор гаманця забезпечує безперешкодну аутентифікацію за допомогою ключів на keys.gemini.com - не потрібні завантаження, розширення або ключі API. Все працює одразу з коробки.

Параметр `appName` потрібний для ідентифікації вашого додатку dApp, а `appIcon` є необов'язковим для брендування вашого підключення.

```tsx
import { geminiWallet } from '@cinagroup/CinaWalletKit/wallets';

const gemini = geminiWallet({
  appName: 'My App',
  appIcon: 'https://my-app.com/logo.png', // Optional
});
```

#### imToken

```tsx
import { imTokenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### ioPay Гаманець

```tsx
import { iopayWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Kaia Гаманець

```tsx
import { kaiaWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Kaikas

```tsx
import { kaikasWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Kraken Гаманець

```tsx
import { krakenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Kresus Гаманець

```tsx
import { kresusWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Ledger Live

```tsx
import { ledgerWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Magic Eden Гаманець

```tsx
import { magicEdenWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### MetaMask

```tsx
import { metaMaskWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### MEW Гаманець

```tsx
import { mewWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Гаманець MeCo

```tsx
import { mecoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Nova Гаманець

```tsx
import { novaWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### OKX Гаманець

```tsx
import { okxWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Omni

```tsx
import { omniWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### OneKey

```tsx
import { oneKeyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### ParaSwap

```tsx
import { paraSwapWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Phantom

```tsx
import { phantomWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Порто

```tsx
import { portoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Rabby Гаманець

```tsx
import { rabbyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Rainbow

```tsx
import { rainbowWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Ramper Гаманець

```tsx
import { ramperWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Готово

```tsx
import { readyWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Ronin Гаманець

```tsx
import { roninWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Safeheron

```tsx
import { safeheronWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Taho

```tsx
import { tahoWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Talisman

```tsx
import { talismanWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Tokenary

```tsx
import { tokenaryWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### TokenPocket

```tsx
import { tokenPocketWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Trust Гаманець

```tsx
import { trustWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Uniswap Гаманець

```tsx
import { uniswapWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Valora

```tsx
import { valoraWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Wigwam

```tsx
import { wigwamWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### xPortal

```tsx
import { xPortalWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### Zerion

```tsx
import { zerionWallet } from '@cinagroup/CinaWalletKit/wallets';
```

#### ZilPay

```tsx
import { zilPayWallet } from '@cinagroup/CinaWalletKit/wallets';
```

### Приклади

#### Порядок

Ось кілька прикладів відображення різних гаманців у різному порядку.

Показати MetaMask разом з універсальними запасними гаманцями.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  metaMaskWallet,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [metaMaskWallet, walletConnectWallet],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

Показати Rainbow, MetaMask та Base разом із загальними запасними гаманцями.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  metaMaskWallet,
  base,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Suggested',
      wallets: [
        rainbowWallet,
        metaMaskWallet,
        base,
        walletConnectWallet,
      ],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

> Нагадування: порядок масиву `wallets` визначає порядок, у якому гаманці будуть відображатися в інтерфейсі користувача.

#### Кілька груп

Ви можете використовувати ключ `groupName` для назви різних груп гаманців. Це корисно, якщо ви хочете спілкуватися з користувачами, які гаманці ви рекомендуєте, а також інші можливі гаманці.

Рекомендуємо Rainbow та MetaMask, але також пропонуємо Base разом із загальнодоступними гаманцями.

```tsx
import { connectorsForWallets } from '@cinagroup/CinaWalletKit';
import {
  rainbowWallet,
  metaMaskWallet,
  base,
  walletConnectWallet,
} from '@cinagroup/CinaWalletKit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, metaMaskWallet],
    },
    {
      groupName: 'Others',
      wallets: [base, walletConnectWallet],
    },
  ],
  { appName: 'CinaWalletKit App', projectId: 'YOUR_PROJECT_ID' },
);
```
